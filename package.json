{
  "name": "tai-openai-hackathon",
  "version": "0.1.0",
  "private": true,
  "description": "Instagram carousel generator for converting X posts into branded HTML carousel templates.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "test": "node --check server.js && node --check public/app.js && node scripts/smoke-test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
